# API server port when running frontend in dev (Parcel). Default 8188.
# Copy to .env and set to match your server port (e.g. 3188).
API_PORT=3188

# Injected at build time into CSP and PWA manifest. Dev default; set for production.
PUBLIC_ORIGIN=http://127.0.0.1:8188
# For production: PUBLIC_ORIGIN=https://your-domain.example.com
