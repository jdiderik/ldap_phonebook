# API server port when running frontend in dev (Parcel). Default 8188.
# Copy to .env and set to match your server port (e.g. 3188).
API_PORT=3188

# Production URL for CSP and PWA (replace your-domain.example.com in index.html and .pwamanifestrc)
# PUBLIC_ORIGIN=https://your-domain.example.com
