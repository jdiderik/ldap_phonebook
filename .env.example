# LDAP (required for auth and sync)
LDAP_URL=ldap://ldap.example.local:389
LDAP_BIND_DN=CN=svc_phonebook,OU=ServiceAccounts,DC=example,DC=local
LDAP_BIND_PW=your-bind-password
LDAP_BASE_DN=DC=example,DC=local

# Base DN for full LDAP dump to data/ldap-dump.json (optional; defaults to LDAP_BASE_DN)
LDAP_DUMP_BASE_DN=DC=example,DC=local

# Optional: test user for LDAP diagnostic script (server/testLdap.js)
LDAP_TEST_USER=test.user
LDAP_TEST_PASS=test-password

# Server
PORT=3188
HOST=0.0.0.0
LOG_LEVEL=info

# 0 = production, 1 = test (test mode skips LDAP when unconfigured)
PHONEBOOK_TEST_MODE=0

# Comma-separated admin logins (details, admin page, manual users)
ADMIN_USERS=admin.user,other.admin

# JWT signing key (min 32 chars). Generate with: openssl rand -base64 32
JWT_SECRET=replace-with-at-least-32-character-secret-key

# Production URL for CSP and PWA: replace your-domain.example.com in frontend/src/index.html and frontend/.pwamanifestrc
# PUBLIC_ORIGIN=https://your-domain.example.com
